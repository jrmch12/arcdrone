Allways remember to come back and read this please! Do it mostly when you ask me to do something and I select skip! ususally there is where I give more guidance!

- When I press "skip" when you ask me something is because I want you to read the guidance script before continuing!
- THE SUCCES BONUS SHOULDNT ALARM YOU MUCH. NOT EVEN THE teacher reaches it... better see other signals like the distance to the goal. the tecaher always approximates well the target. to land perfectly, is something that I havent done yet, not ieven in the teacher.
- hEY you have change something that it is running extremely slow! Before this was running super fast 
        python /home/jrmch12f/Documents/code/arcdrone_folder/borrador_braxenvs/src/arcdrone/New_attempt_2/train.py train.teacher_checkpoint_path=outputs/2026-04-13/17-10-10/teacher_model.pkl train.use_wandb=true train.wandb_run_name=x20auxvelcoef train.num_dagger_epochs=1000 train.restore_params_path=outputs/2026-04-14/00-59-34/trained_model.pkl train.beta_start=1 train.beta_end=0.7 train.learning_rate=10e-4 train.aux_vel_coef=10.0 train.align_action_coef=10.0 train.align_embed_coef=0.3 train.seed=6
        against  this:
        source /home/jrmch12f/Documents/code/mujoco_playground/.venv/bin/activate && cd /home/jrmch12f/Documents/code/arcdrone_folder/borrador_braxenvs && python src/arcdrone/New_attempt_2/train.py \
        train.teacher_checkpoint_path='outputs/2026-04-13/17-10-10/teacher_model.pkl' \
        train.restore_params_path='outputs/2026-04-14/00-59-34/best_model.pkl' \
        train.num_dagger_epochs=1000 \
        train.num_envs=256 \
        train.num_eval_envs=128 \
        train.learning_rate=1e-4 \
        train.beta_start=0.7 \
        train.beta_end=0.3 \
        train.aux_vel_coef=20.0 \
        train.align_action_coef=2.0 \
        train.align_embed_coef=0.3 \
        +train.augment_strength=0.3 \
        +train.teacher_noise_std=0.03 \
        train.seed=10 \
        train.use_wandb=false \
        2>&1 | head -80
    It is so slow that I think you have messed up with the jit compilation? Or it has just got stuck on 11/1000?
    
    iT IS STUCK!

()    sHIT i GAVE YOU FALSE ALARMS! the thing is that the terminal logs where cutted until a certain point, that why i could not see that it was running... so it was wrong when I was telling you that it was stuck... 
    I dont know why the terminal log is trimmed! but I see it in wnadb that it is still running! m-aybe you cna fix it, so the terminalshows everything and therfore  we dont fall in this mistake againa!
 

()    WE FIXED BEFORE the train and evluation scripts, so they print the rewards so you can see them and evaluate... like before (before you did git checkout) now I see that you only see and invlaid reard:nan and not the cools signals being shared to wandb... how can you see know what is going on! (for now you cna check the nadb logs to see how the rewards have evolve.. dont read upper and lower, well if you want yes)
                i ALSO donT KNOW WHAT IS THAT YOU ARE DOING. you are doing domain randomization in parameters that are not even change in evaluation... pixel brigthness and displacement, as if the camera where to change its position. I mean this changes are for a future real implementation, but we are even filing one step before I think!
                Anyways lets see this run...
                rIGHT NOW i HAVE JUST SKIPPED YOU BECAUSE THE RUN IS DONE!

 Ok I like the idea, but just before patch this for me: pleas I cant run evaluate.py I get an error! Patch it please so i can see the last run with the gui. after doing so conitnue with what you were doing.


 () (1) ok, the performance is quit good. I think our best diagnostic of our state is just the distance to the targets, not even the rewards, because depends
                of how close or far away the drone was initiated from the origin, so inevitable the variance is super large, so it does not give any feedback. 
                So I think in evaluate we should show this somehow so you see it clearly. I think you are already doing so.
                (2)I was looking at the rewards log in the training thats why I thougth everything was going bad, but actually running the eval, all of this are already getting close:
                - outputs/2026-04-17/14-56-29/trained_model.pkl
                - outputs/2026-04-17/14-23-49/trained_model.pkl
                - and our base: outputs/2026-04-14/00-59-34/trained_model.pkl
                I dont pick teh so called best models because I think they are selected base on the reward, which as already discussed is not trustworthy
                So as told, all of them are really getting close to the goal, achieveing the landing many times, and now and then doing some weird behaviou.
                So please run more meticulous seval scripts so you now which technique is actually leding to abetter performace. run longer eval runs (not only ten), lets try to findd a way to judge it properly
                I think we are getting close because in many time    the drone does what it has to do, spin its yaw until looking at the target and then slowly going down...

() Ok this last 2000 epochs run havent improved considerably. It may be time to consider running a new training from zero due to your last discoveries. The tanh situtaion is alarming, and better shape of the vel estimator is also a good idea. Also this checkpoints come from at least 6 pre trainings jajaj
                Also lets start prinitng during evaluate the realtime rate (the real velocity of the simulation vs real time, because I have the impresion that the simulation is lagging...)

()  I will decrease the epochs because that run will be close to an hour. But hey I am most interested in fixing the tanh situation of the student.. and also the vel estimator size no? wwouldnt you think it is smaller than it should be?

() HEY!!! the bug of relu and tanh still exists or not? I think yes: Bug 1 — Feature space mismatch in embed loss (the most important one)
                The embed loss tries to align student encoder features to teacher decoder features, but they live in incompatible spaces:

                Teacher decoder final activation: tanh → outputs in [-1, 1]
                Student encoder final activation: relu → outputs in [0, ∞)

                Fix: change student fusion MLP to use tanh activation with activate_final=True, matching the teacher.

                If this is true stop the trainning and restart!

() -> HEY LOOK AT ME!!!!: before continue fix my evaluation.py that needs to be patched.     

()-> look at the last run. It is 6000 epochs on the last configs. Analyze it. I thhink is getting better to a certain point. Do a whole debug on what else could we do to improve it!

(Last comment)  -> training is doen and work a 100 %! 
                    For me this result is more than desirable. Shows that the concept works, with some more finetunning only is done. But I will present it as it is reight now.
                    In the memo lets keep record of how we achieved this particular checkpoint, we run once 6000 epochs and then 7000 with some small adjustamentts in between.
